apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: litellm-proxy
spec:
  port:
    targetPort: 4000
  to:
    kind: Service
    name: litellm-proxy
    weight: 100
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
